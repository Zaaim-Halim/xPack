/**
 * Types bound directly from a project's POM.
 *
 * <p>Part of the plugin's public surface: renaming a field here renames an
 * element in every build that configures it.
 */
package io.xpack.config;
