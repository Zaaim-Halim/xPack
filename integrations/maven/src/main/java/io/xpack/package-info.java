/**
 * The goals a project binds in its POM.
 *
 * <p>Each one does a single step of turning a Maven build into an xPack
 * installation, and every step that produces an artefact does it by running
 * the xPack command line rather than reimplementing what it does.
 */
package io.xpack;
