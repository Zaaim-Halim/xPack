/**
 * Helpers the goals are built from.
 *
 * <p>Not API. Nothing here is named in a POM, so these types may change
 * without it being a breaking change for anyone's build.
 */
package io.xpack.internal;
